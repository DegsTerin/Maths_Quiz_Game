# Version Zero (`v0`)

This folder contains the original initial version of the Maths Quiz Game project.

It is preserved here as the project's version zero (`v0`) for reference and historical comparison with the current repository structure.
